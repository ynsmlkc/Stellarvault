[package]
name = "stellar-vault"
version = "0.1.0"
edition = "2021"
rust-version = "1.81"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "23.0" }

[dev-dependencies]
soroban-sdk = { version = "23.0", features = ["testutils"] }
