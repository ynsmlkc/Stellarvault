# Dependencies
node_modules/
target/

# Build
dist/
build/
.next/

# Environment
*.env
!.env.example

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Circom build artifacts
circuits/build/
*.r1cs
# NOTE: web/public/zk-wasm/*.zkey and *.wasm are runtime assets tracked via Git LFS
# (see .gitattributes). Do not add a global *.zkey ignore back — it would drop them.

# Local config
.claude/

# The original throwaway deployer-API spike. Later spikes under spike/ are
# evidence for decisions (BN254 availability, confidential-token integration)
# and are tracked — they are cited from READMEs and commit messages.
spike/deployer-api/
spike/*/target/

# bb.js browser build, vendored at build time by web/scripts/vendor-bb.mjs
web/public/vendor/bb/
